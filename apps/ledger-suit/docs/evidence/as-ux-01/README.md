# AS-UX-01 verification — 2026-09-19

The actual Ledger application has one Transactions workspace and a default account tree. Preview captures use the preserved local Beta demo workspace; en/ar test captures use isolated test accounts and real posted transactions on the disposable backend. Arabic tests use dark mode; English tests use light mode. No hosted data is included.

## Results

| Check | Actual result |
|---|---|
| Root `pnpm test` | 44 passed, including five new tree cases |
| Root `pnpm check` | PASS; canonical tokens match, boundaries pass, 80 historical migrations unchanged |
| Ledger `lint`, `typecheck`, `build` | PASS |
| Combined eight browser specs below | 38 passed; one usage dialog-reopen test failed on close timing |
| Corrected reopen test, `--repeat-each 3` | 3 passed; waits for the dialog to disappear before reopening |
| Expired trial/lapsed workspace cases | 2 passed |
| Distinct browser scenarios with passing evidence | 41; do not sum repeated attempts as distinct coverage |
| Preview capture | PASS; clean Transactions, Arabic tree, 390px mobile width, no page errors |
| SQL | NOT_RUN for this UX package; no SQL/schema changes |

New browser coverage checks real owner posting, exact parent/child balances and statement access, subaccount defaults, filters surviving reload/detail dismissal/save, old route redirects, pagination/filter reset, validation/retry, actual viewer permissions, and late tenant responses. Five pure-helper cases cover empty headings, historical parent/contra totals above 2^53, ancestor search, malformed parent links and conservative loan grouping. Delayed/error/query fixtures prove client behavior; they do not replace database authorization tests.

The final real-posting run found and verified a production-code fix: refreshing organization reads preserves mounted account selector options after save. Earlier test iterations also corrected RPC fixtures to provide their own revenue/expense accounts, removed unrelated dashboard readiness waits, and handled existing dirty-form confirmation. The help button no longer overlays the paginator. Historical evidence images generated by regressions were restored; this directory contains the new evidence only.

## Reproduce

From the monorepo root, use the maintained local backend setup and the tracked Playwright configuration (API 60321; rejects hosted URLs). Build first, then:

```sh
pnpm test
pnpm check
pnpm --filter @building-suit/ledger-suit lint
pnpm --filter @building-suit/ledger-suit typecheck
pnpm --filter @building-suit/ledger-suit build
pnpm --filter @building-suit/ledger-suit exec playwright test tests/e2e/workspace-ux.spec.ts tests/e2e/accounts-table.spec.ts tests/e2e/account-activity.spec.ts tests/e2e/account-groups.spec.ts tests/e2e/account-nature.spec.ts tests/e2e/core-finance.spec.ts tests/e2e/statement-classification.spec.ts tests/e2e/usage.spec.ts
pnpm --filter @building-suit/ledger-suit exec playwright test tests/e2e/trial-billing.spec.ts tests/e2e/acquisition.spec.ts --grep 'expired Trial|lapsed workspace'
```

This execution reused the task-owned `ledger-account-activity-20260919` local backend on API 65321/database 65322, confirmed by container labels. An ignored copy of the Playwright configuration changes only local ports (app 3261; read-only/repeat runs 3262), source-relative paths and the loopback guard. The backend was not reset. No SQL deployments were needed. Source/log/image SHA-256 values and run outcomes are in [verification.json](verification.json); raw logs remain ignored and contain no review-required artifacts.

The review preview runs at `http://127.0.0.1:3260/transactions` and `/accounts`, using `owner@beta.test` / `ledgersuit`. `preview-transactions.png`, `preview-tree-ar.png` and `preview-tree-mobile.png` show its preserved demo data. Other images show the automated en/ar scenarios. No accountant UAT, merge or hosted deployment is claimed.
