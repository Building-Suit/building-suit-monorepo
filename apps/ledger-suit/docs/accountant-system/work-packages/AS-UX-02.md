# AS-UX-02 — CSV modal, template and selected-language files

2026-09-19 — READY_FOR_REVIEW. Founder request: open transaction CSV import in a modal, provide a downloadable template, and use the selected Arabic/English language throughout CSV import/export.

Base: merged PR #13, `stg` at `1ca570993359f7a8637195e1f8e567748baaf59a`; branch `codex/ledger-suit/localized-csv`. Independent Shop worktrees remain excluded and untouched. Preserve the existing import validation/confirmation RPCs and export authorization; no database policy or schema change is planned.

Bounded delivery: shared-dialog import from Transactions with filter/focus preservation and legacy-link redirection; localized template, automatic Arabic/English column matching and canonical value preparation that retains source cells; localized headers/accounting labels for all five existing report exports. Keep dates, exact amounts, identifiers and user-entered names intact. Verify real localized import/duplicate protection and actual CSV downloads, permission/plan/error/tenant states, desktop/mobile/RTL, unit/lint/typecheck/build and existing affected regressions. Push a review PR; no merge or hosted deployment.


Delivered: the import dialog preserves Transactions filters, protects an uploaded file, restores keyboard focus and blocks stale responses after navigation/tenant changes. `/imports` redirects to the dialog. Templates, filenames, headers and accounting labels follow the selected locale. Column matching accepts either language; Arabic digits/type labels are normalized in additional source columns without overwriting original cells. Report serialization preserves exact amounts, protected text, references, dates and classification IDs, including empty reports.

Verification exposed and fixed two application defects: removing the modal before its close transition prevented focus restoration; a forced quota refresh could join an older in-flight read and retain stale usage. The dialog stays mounted for the shared close lifecycle, and quota refresh requests coalesce into a fresh follow-up read. A deterministic browser case holds the earlier response while reopening and verifies the newer allowance.

PASS: 49 unit tests, 27 browser cases (25 combined +2 read-only), Ledger lint/typecheck/build, root check and the local preview capture. Real en/ar cases download/edit/upload the template, post two transactions, re-import them without creating duplicates, preserve raw cells and filters, then download all five reports with localized headers and labels. Earlier fixture setup incorrectly supplied an unsupported account argument and duplicated automatically generated categories; corrected fixtures use the existing contracts. One parallel read-only run exceeded its account-loading timeout; the final serial run passes unchanged. No unresolved failures.

[Evidence, commands, screenshots and downloaded templates](../../evidence/as-ux-02/README.md). Local backend `ledger-account-activity-20260919` (API 65321/database 65322), verified by container labels and reused without reset. Review preview: `http://127.0.0.1:3270/transactions`, fixture `owner@beta.test` / `ledgersuit`. No hosted writes, SQL changes, merge or deployment. SQL was not rerun; earlier database-test counts are not claimed anew. No approval blockers; accountant UAT and release acceptance are not claimed. Stop for review after publishing.
