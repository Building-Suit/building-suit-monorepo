# Copy to .env. Use the verified environment map in docs/architecture.
APP_URL="http://localhost:3001"
# The current portal key is configured in nuxt.config.ts.
# The isolated local API is shared with Ledger; Shop requires its current baseline.
SUPABASE_URL="http://127.0.0.1:59321"
# Fill with the local publishable/anon key from supabase start.
SUPABASE_KEY=""
